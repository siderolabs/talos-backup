# commit to be tagged for the new release
commit = "HEAD"

project_name = "talos-backer-upper"
github_repo = "talos-systems/talos-backer-upper"
match_deps = "^github.com/(talos-systems/[a-zA-Z0-9-]+)$"

# previous = -
# pre_release = true

# [notes]
